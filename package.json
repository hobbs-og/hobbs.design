{
  "name": "hobbs.design",
  "version": "5.1.0",
  "private": true,
  "description": "The hobbs.design site. Tokens and CSS primitives come from @hobbs-og/design-system; this repo holds the pages, the organisms, and its own component-token layer.",
  "type": "module",
  "scripts": {
    "build": "npm run build:tokens && npm run build:icons && npm run vendor",
    "build:tokens": "style-dictionary build --config style-dictionary.config.mjs",
    "watch:tokens": "style-dictionary build --config style-dictionary.config.mjs --watch",
    "build:icons": "hobbs-icons --scan . --out src/public/icons.svg",
    "vendor": "node tools/vendor.mjs",
    "serve": "node tools/serve.mjs",
    "postinstall": "node tools/vendor.mjs"
  },
  "devDependencies": {
    "@hobbs-og/design-system": "github:hobbs-og/hobbs-design-system#v1.2.0",
    "style-dictionary": "^4.3.0"
  }
}
