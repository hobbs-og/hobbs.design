# ---- Secrets ----
# Never commit credentials. Config is loaded from the environment.
.env
.env.*
!.env.example
*.pem
*.key
secrets.php
config.local.php

# ---- OS / editor noise ----
.DS_Store
**/.DS_Store
Thumbs.db
.vscode/
.claude/settings.local.json

# ---- Dependencies ----
node_modules/

# vendor/ is NOT ignored, on purpose. It holds @hobbs-og/design-system,
# copied out of node_modules by `npm run vendor`. The cPanel deploy only
# copies files — it runs no npm and no build — so the design system has to
# be committed here or the deployed site has no stylesheet.

# ---- Archived previous version (kept on disk, not in history) ----
_V4.0/
xx_graveyard/

# ---- Pre-optimization image originals (local safety copy) ----
_image-originals/

# ---- Source design files ----
mocks/*.psd
*.psd
*.ai
*.sketch
*.fig

# ---- Build noise ----
*.css.map
*.log

# ---- Scratch / experiments ----
test.php
test_mail.php
send_mail_test.php
curl_test.php
test.html
grid.html
